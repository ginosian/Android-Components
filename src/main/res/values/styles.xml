<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.NoActionBar.StatusBarColored" parent="AppTheme.NoActionBar" />

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="fade_animation">
        <item name="@android:windowEnterAnimation">@anim/fade_out</item>
        <item name="@android:windowExitAnimation">@anim/fade_in</item>
    </style>

    <!-- Activity that uses list component should include this style in manifest -->
    <style name="Widgets.Components.ListComponent" parent="AppTheme.NoActionBar">
        <item name="android:editTextColor">@color/black87</item>
        <!--<item name="android:textColorHint">@color/list_component_text</item>-->
    </style>
</resources>
